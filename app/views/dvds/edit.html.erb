<%if user_signed_in? %>   
  <% if current_user.role == "admin" || current_user.role == "mitarbeiter" %>

	<h1>DVD ausleihen</h1>

	<%= form_for(@dvd) do |f| %>
  	  <% if @dvd.errors.any? %>
    	<div id="error_explanation">
      	  <h2><%= pluralize(@dvd.errors.count, "error") %> prohibited this dvd from being saved:</h2>

      	  <ul>
      	    <% @dvd.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
      	    <% end %>
      	  </ul>
    	</div>
  	  <% end %>
  
    <div class="actions" value="Ausleihen">
      <%= f.submit %>
  	</div>
  <% end %>

  <%= link_to 'Info', @dvd %> |
  <%= link_to 'Zurück', dvds_path %>

<% else %>

	Nicht zulässig!

  <% end %>
<% else %>
Bitte logge dich ein!
<% end %>